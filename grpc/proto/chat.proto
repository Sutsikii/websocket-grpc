syntax = "proto3";

package chat;

service ChatService {
  rpc SendMessage (Message) returns (Ack);
}

message Message {
  string username = 1;
  string content = 2;
}

message Ack {
  string status = 1;
}
